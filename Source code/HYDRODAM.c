#include<stdio.h>

int main (void) {
    freopen("input.txt", "r", stdin);
    int T = 0;
    scanf("%d ", &T);
    int a[T];

    for (int t = 0; t < T; t++) {
        scanf("%d ", &a[t]);
    }

    int volume = 0;
    int temp = 0;
    for (int i = 0; i < T; i++) {
        
    }

    printf("%d\n", max);
    return 0;
}